#
#   Jar files
#

# this is for enabling GVector and GMatrix
${LIBS}/vecmath.jar

# this is for enabling PDFLib
${LIBS}/lib/pdflib.jar
${LIBS}:${DEV_NATIVELIB_DIR}


# this is for enabling LAPACK
${LIBS}/jlapack-0.6.jar

# this is for enabling XML
${LIBS}/xml4j.jar
${LIBS}/xmlParserAPIs.jar

# this is for enabling JBOSS
${LIBS}/jbossall-client.jar

# this is for enabling LOG4J
${LIBS}/log4j.jar

# this is for enabling DTD validation
${LIBS}/isorelax.jar
${LIBS}/msv.jar
${LIBS}/relaxngDatatype.jar
${LIBS}/xsdlib.jar

